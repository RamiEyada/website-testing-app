// src/progressTracker.js

let progress = 0; // Example progress variable

// Simulate progress increment (you'll replace this with real progress tracking logic)
export function updateProgress(value) {
    progress = value;
}

export function getProgress() {
    return progress;
}